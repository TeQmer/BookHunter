# 🚀 Деплой BookHunter на Beget

Специальная инструкция для деплоя на сервер Beget с доменом на том же хостинге.

---

## ✅ Преимущества Beget

- ✅ **SSL сертификат в 1 клик** — через панель управления Beget (Let's Encrypt)
- ✅ **Простая настройка DNS** — можно добавить A-запись через панель
- ✅ **SSH доступ** — есть доступ по SSH
- ✅ **Автоматическое обновление SSL** — Beget обновляет сертификаты автоматически

---

## 📝 Порядок действий (ВАЖНО!)

Перед деплоем нужно выполнить эти шаги:

1. **Купить сервер на Beget** (VPS или выделенный сервер)
2. **Получить IP адрес сервера** (будет в панели Beget)
3. **Настроить DNS** — добавить A-запись с IP сервера (см. раздел "Настройка DNS")
4. **Подождать 5-30 минут** — DNS обновится (проверьте через nslookup)
5. **Деплоить приложение** — следуйте инструкции ниже (сначала приложение!)
6. **Проверить работу по IP** — http://YOUR_SERVER_IP:8080/web
7. **Установить SSL** — через панель Beget (1 клик) — ПОСЛЕ того, как приложение работает
8. **Настроить Telegram Bot** — ПОСЛЕ установки SSL (требуется HTTPS)

---

## 📋 Требования к серверу Beget

### Рекомендуемая конфигурация:
- **CPU:** 2+ ядра
- **RAM:** 4+ GB
- **Disk:** 20+ GB SSD
- **OS:** Ubuntu 20.04+ / Debian 11+

---

## 🚀 Быстрый деплой на Beget (5-10 минут)

### 1. Подключение к серверу Beget

```bash
ssh root@85.198.103.166
# Или через SSH ключ:
ssh -i ~/.ssh/beget_key user@your-server-ip.beget.tech
```

**Где найти данные для SSH:**
1. Зайдите в панель Beget
2. Раздел "Хостинг" → "SSH доступ"
3. Там будут данные для подключения

---

### 2. Установка Docker и Docker Compose

Beget не устанавливает Docker по умолчанию, поэтому нужно установить:

```bash
# Обновление системы
sudo apt-get update && sudo apt-get upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo apt-get install docker-compose-plugin -y

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Проверка установки
docker --version
docker compose version
```

---

### 3. Клонирование проекта

```bash
# Клонирование репозитория
git clone https://github.com/TeQmer/BookHunter.git
cd BookHunter
```

---

### 4. Настройка переменных окружения

```bash
# Копирование примера .env для продакшена
cp .env.prod.example .env

# Редактирование .env
nano .env
```

**Обязательные изменения в .env:**
```env
# База данных
DATABASE_URL=postgresql+asyncpg://bookuser:your_secure_password_here@postgres:5432/book_discounts
POSTGRES_USER=bookuser
POSTGRES_PASSWORD=your_secure_password_here

# Redis
REDIS_URL=redis://:your_redis_password_here@redis:6379/0
REDIS_PASSWORD=your_redis_password_here

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Google Sheets
GOOGLE_SHEET_ID=your_sheet_id_here
# Убедитесь, что все GOOGLE_CREDENTIALS_* переменные заполнены!

# Админ-панель
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_admin_password_here

# Mini App URL (ВАЖНО!)
MINI_APP_URL=https://mybook-hunter.ru/telegram

# CORS (ВАЖНО!)
ALLOWED_ORIGINS=https://mybook-hunter.ru,https://www.mybook-hunter.ru
ALLOWED_HOSTS=mybook-hunter.ru,www.mybook-hunter.ru

# Безопасность
SECRET_KEY=your_secret_key_here
DEBUG=False
LOG_LEVEL=INFO
```

---

### 5. Настройка Google Credentials через переменные окружения

⚠️ **Важно:** Мы НЕ используем файл `credentials.json`, только переменные окружения!

Убедитесь, что в `.env` заполнены все эти переменные:
```env
GOOGLE_CREDENTIALS_TYPE=service_account
GOOGLE_CREDENTIALS_PROJECT_ID=your_project_id
GOOGLE_CREDENTIALS_PRIVATE_KEY_ID=your_private_key_id
GOOGLE_CREDENTIALS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_CREDENTIALS_CLIENT_EMAIL=your_service_account_email@project-id.iam.gserviceaccount.com
GOOGLE_CREDENTIALS_CLIENT_ID=your_client_id
GOOGLE_CREDENTIALS_AUTH_URI=https://accounts.google.com/o/oauth2/auth
GOOGLE_CREDENTIALS_TOKEN_URI=https://oauth2.googleapis.com/token
GOOGLE_CREDENTIALS_AUTH_PROVIDER_CERT_URL=https://www.googleapis.com/oauth2/v1/certs
GOOGLE_CREDENTIALS_CLIENT_CERT_URL=https://www.googleapis.com/robot/v1/metadata/x509/your_service_account_email%40project-id.iam.gserviceaccount.com
GOOGLE_SHEET_ID=your_sheet_id
```

---

### 6. Запуск приложения

```bash
# Запуск контейнеров через docker-compose.prod.yml
docker compose -f docker-compose.prod.yml up -d

# Проверка статуса
docker compose -f docker-compose.prod.yml ps

# Просмотр логов
docker compose -f docker-compose.prod.yml logs -f app
```

---

### 7. ⚠️ ВАЖНО: Настройка порта приложения

По умолчанию приложение работает на порту 8000, но Beget Nginx ожидает приложение на порту 8080 или другом.

**Выберите один из вариантов:**

#### Вариант А: Изменить порт в docker-compose.prod.yml (РЕКОМЕНДУЕТСЯ)

Отредактируйте `docker-compose.prod.yml`:

```bash
nano docker-compose.prod.yml
```

Найдите раздел `services` → `app` → `ports` и измените:

```yaml
services:
  app:
    ports:
      - "8080:8000"  # 8080 на хосте, 8000 в контейнере
```

Затем перезапустите:
```bash
docker compose -f docker-compose.prod.yml down
docker compose -f docker-compose.prod.yml up -d
```

#### Вариант Б: Настроить проксирование через Nginx Beget

Если вы не хотите менять порт, настройте проксирование в панели Beget:

1. Зайдите в панель Beget
2. Раздел "Сайты" → ваш сайт
3. "Настройка Nginx"
4. Добавьте правило проксирования:

```nginx
location / {
    proxy_pass http://localhost:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

---

### 8. Настройка Telegram Bot

⚠️ **Важно:** Настраивайте бота ПОСЛЕ установки SSL, так как Web App требует HTTPS!

1. Перейдите к [@BotFather](https://t.me/BotFather)
2. Откройте вашего бота
3. Настройте Web App:
   ```
   /setmenubutton
   Выберите вашего бота
   /newapps
   Текст кнопки: 📚 BookHunter
   URL: https://mybook-hunter.ru/telegram
   ```

**Примечание:** Если SSL ещё не установлен, вы можете временно использовать HTTP для тестирования, но в продакшене обязательно нужен HTTPS!

---

## ✅ Проверка работы по IP (ДО настройки DNS и SSL)

⚠️ **ВАЖНО:** Перед настройкой DNS и SSL убедитесь, что приложение работает по IP!

### Как проверить:

```bash
# Замените YOUR_SERVER_IP на реальный IP вашего сервера
curl http://YOUR_SERVER_IP:8080/api/health
```

Или откройте в браузере:
```
http://YOUR_SERVER_IP:8080/web
```

**Ожидаемый результат:**
- ✅ Главная страница сайта открывается
- ✅ API Health возвращает `{"status":"ok"}`
- ✅ Нет ошибок в логах (`docker compose -f docker-compose.prod.yml logs app`)

**Если НЕ работает:**
- Проверьте логи: `docker compose -f docker-compose.prod.yml logs app`
- Проверьте статус: `docker compose -f docker-compose.prod.yml ps`
- Проверьте, что порт 8080 открыт: `sudo ufw status`

---

## 🔒 SSL Сертификат на Beget

### ⚠️ ВАЖНО: SSL устанавливается ПОСЛЕ настройки DNS!

Beget предоставляет бесплатный SSL сертификат (Let's Encrypt), который:

1. ✅ **Устанавливается в 1 клик** через панель управления
2. ✅ **Автоматически обновляется** — Beget обновляет сертификаты
3. ✅ **Поддерживает все современные браузеры**
4. ✅ **Бесплатный и бессрочный**

### Когда устанавливать SSL:

✅ **ПОСЛЕ того, как:**
- DNS настроен (A-запись добавлена)
- DNS обновился (nslookup возвращает правильный IP)
- Приложение запущено на сервере

❌ **НЕ устанавливайте ДО того, как:**
- DNS не настроен
- DNS ещё не обновился

### Как установить SSL:

1. Зайдите в панель Beget
2. Раздел "Сайты" → ваш сайт
3. Нажмите "Установить SSL сертификат"
4. Выберите "Бесплатный сертификат Let's Encrypt"
5. Нажмите "Установить"

### Как проверить, что SSL работает:

1. Зайдите на ваш домен: `https://mybook-hunter.ru`
2. Если видите замок 🔒 в адресной строке — SSL работает
3. Или проверьте через сервис: https://www.ssllabs.com/ssltest/

---

## 🌐 Настройка DNS на Beget

### ⚠️ ВАЖНО: DNS нужно настроить вручную!

Поскольку домен и сервер на Beget, вам нужно добавить A-запись:

### Шаг 1: Получите IP адрес сервера

1. Зайдите в панель Beget
2. Раздел "Хостинг" → ваш сервер
3. Скопируйте IP адрес (например: `185.100.100.100`)

### Шаг 2: Добавьте A-запись

1. Зайдите в панель Beget
2. Раздел "Домены" → ваш домен
3. Нажмите "DNS записи"
4. Добавьте запись:

| Тип | Имя | Значение |
|-----|-----|----------|
| A | `@` (или оставьте пустым) | `185.100.100.100` (IP вашего сервера) |

5. Нажмите "Сохранить"

### Шаг 3: Проверьте DNS

Подождите 5-30 минут и проверьте:

```bash
# Проверка A-записи
nslookup mybook-hunter.ru

# Проверка через dig (если установлен)
dig mybook-hunter.ru A
```

Должен вернуть IP вашего сервера Beget.

### ⚠️ Что делать, если DNS не обновляется:

1. Подождите дольше (иногда до 24 часов)
2. Очистите кэш DNS на компьютере
3. Проверьте, что A-запись добавлена правильно
4. Обратитесь в поддержку Beget

---

## 🔄 Обновление проекта на Beget

### Через Git

```bash
# Подключение к серверу
ssh user@your-server-ip.beget.tech

# Переход в директорию проекта
cd BookHunter

# Получение обновлений
git pull origin main

# Остановка контейнеров
docker compose -f docker-compose.prod.yml down

# Пересборка и запуск
docker compose -f docker-compose.prod.yml up -d --build

# Проверка статуса
docker compose -f docker-compose.prod.yml ps
```

### Скрипт автоматического обновления

Создайте файл `update.sh`:

```bash
#!/bin/bash
echo "🔄 Обновление BookHunter..."

# Получение обновлений
git pull origin main

# Остановка контейнеров
docker compose -f docker-compose.prod.yml down

# Пересборка и запуск
docker compose -f docker-compose.prod.yml up -d --build

# Очистка старых образов
docker image prune -f

echo "✅ Обновление завершено!"
```

Сделайте исполняемым:
```bash
chmod +x update.sh
./update.sh
```

---

## 📊 Мониторинг на Beget

### Через Docker

```bash
# Логи приложения
docker compose -f docker-compose.prod.yml logs -f app

# Логи всех сервисов
docker compose -f docker-compose.prod.yml logs -f

# Логи за последние 100 строк
docker compose -f docker-compose.prod.yml logs --tail=100 app
```

### Через панель Beget

Beget предоставляет мониторинг ресурсов:
1. Зайдите в панель Beget
2. Раздел "Хостинг" → "Мониторинг"
3. Там можно увидеть CPU, RAM, Disk usage

---

## 🐛 Решение проблем на Beget

### Контейнер не запускается

```bash
# Просмотр логов
docker compose -f docker-compose.prod.yml logs app

# Перезапуск контейнера
docker compose -f docker-compose.prod.yml restart app

# Пересборка
docker compose -f docker-compose.prod.yml up -d --build
```

### Ошибка подключения к базе данных

```bash
# Проверка статуса PostgreSQL
docker compose -f docker-compose.prod.yml logs postgres

# Перезапуск PostgreSQL
docker compose -f docker-compose.prod.yml restart postgres

# Проверка подключения
docker compose -f docker-compose.prod.yml exec postgres psql -U bookuser -d book_discounts
```

### Проблемы с портами (Beget Nginx)

Если приложение недоступно по домену:

1. Проверьте, что порт открыт:
```bash
# На сервере
sudo ufw status
# Если нужно, откройте порт:
sudo ufw allow 8080/tcp
```

2. Проверьте настройки Nginx в панели Beget

3. Проверьте логи Nginx Beget:
```bash
# На сервере
sudo tail -f /var/log/nginx/error.log
```

---

## 🔐 Безопасность на Beget

### Firewall уже настроен

Beget уже настроил firewall, но можно проверить:

```bash
sudo ufw status
```

### Резервное копирование

Beget предоставляет автоматические бэкапы, но можно создать свой:

```bash
# Создание скрипта бэкапа
nano backup.sh
```

```bash
#!/bin/bash
BACKUP_DIR="/home/user/bookhunter_backups"
DATE=$(date +%Y%m%d_%H%M%S)

# Создание директории
mkdir -p $BACKUP_DIR

# Бэкап базы данных
docker compose -f docker-compose.prod.yml exec -T postgres pg_dump -U bookuser book_discounts > $BACKUP_DIR/db_$DATE.sql

# Бэкап .env файла
cp .env $BACKUP_DIR/env_$DATE

# Удаление старых бэкапов (старше 7 дней)
find $BACKUP_DIR -type f -mtime +7 -delete

echo "✅ Бэкап создан: $BACKUP_DIR/db_$DATE.sql"
```

Добавьте в cron:
```bash
crontab -e
# Каждый день в 2 часа ночи
0 2 * * * /home/user/BookHunter/backup.sh
```

---

## ✅ Проверка после деплоя на Beget

### Шаг 1: Проверка по IP (сразу после деплоя)

1. **Главная страница:** http://YOUR_SERVER_IP:8080/web (или другой порт, если настроили)
2. **API Health:** http://YOUR_SERVER_IP:8080/api/health

**Если работает — переходим к следующему шагу!**

---

### Шаг 2: Настройка DNS и SSL

Следуйте разделам выше:
- "Настройка DNS на Beget"
- "SSL Сертификат на Beget"

---

### Шаг 3: Проверка по домену (после DNS и SSL)

3. **Главная страница:** https://mybook-hunter.ru/web
4. **API Health:** https://mybook-hunter.ru/api/health
5. **Админ-панель:** https://mybook-hunter.ru/admin
6. **Mini App:** https://mybook-hunter.ru/telegram
7. **SSL сертификат:** Замок 🔒 в адресной строке
8. **Telegram Bot:** Отправьте /start боту

---

## 📞 Поддержка Beget

Если возникнут проблемы с сервером или хостингом:

1. **Панель управления:** https://panel.beget.com
2. **База знаний:** https://beget.com/ru/knowledge
3. **Техподдержка:** через тикеты в панели Beget

---

## 🎯 Краткий чек-лист для Beget

### Перед деплоем:
- [ ] Купили сервер на Beget
- [ ] Получили IP адрес сервера

### DNS (настраиваете ПЕРЕД деплоем):
- [ ] Настроили DNS (добавили A-запись)
- [ ] Проверили DNS (nslookup возвращает правильный IP)
- [ ] Подождали 5-30 минут после настройки DNS

### Деплой приложения:
- [ ] Подключились к серверу по SSH
- [ ] Установили Docker и Docker Compose
- [ ] Клонировали проект
- [ ] Настроили `.env` файл (из .env.prod.example)
- [ ] Заполнили все `GOOGLE_CREDENTIALS_*` переменные
- [ ] Запустили контейнеры (`docker compose -f docker-compose.prod.yml up -d`)
- [ ] Настроили порт (8080 или Nginx проксирование)
- [ ] Проверили, что приложение работает по IP (http://IP:8080/web)

### После деплоя (DNS и SSL):
- [ ] Установили SSL сертификат через панель Beget
- [ ] Проверили, что приложение доступно по домену (https://mybook-hunter.ru/web)
- [ ] Проверили SSL (замок 🔒 в адресной строке)
- [ ] Настроили Telegram Bot Web App
- [ ] Настроили бэкапы

---

**Удачи с деплоем на Beget!** 🚀
