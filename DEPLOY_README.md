# 📚 BookHunter - Инструкции по деплою

## 🚀 Быстрый выбор инструкции

| Ваша задача | Инструкция |
|-------------|------------|
| **Первый деплой на сервер** | [DEPLOY_TO_SERVER.md](DEPLOY_TO_SERVER.md) |
| **Настройка CI/CD (GitHub Actions)** | [CI_CD_GUIDE.md](CI_CD_GUIDE.md) |
| **Быстрый старт CI/CD** | [QUICK_DEPLOY.md](QUICK_DEPLOY.md) |
| **Очистка проекта** | См. ниже |

---

## 🧹 Очистка проекта перед деплоем

### Windows:
```powershell
.\cleanup.ps1
```

### Linux/Mac:
```bash
chmod +x cleanup.sh
./cleanup.sh
```

### Что очищается:
- ✅ Python cache (`__pycache__`, `.pyc`)
- ✅ Логи (`logs/*.log`)
- ✅ Временные файлы (`.tmp`, `.temp`)
- ✅ Docker cache
- ✅ Test cache

---

## 📋 Чек-лист перед деплоем

### Локально:
- [ ] Проект очищен (`./cleanup.sh` или `.\cleanup.ps1`)
- [ ] Все изменения закоммичены
- [ ] `.env` файл не попадет в Git (проверен в `.gitignore`)
- [ ] `credentials.json` не попадет в Git (проверен в `.gitignore`)
- [ ] Проект локально работает

### На сервере:
- [ ] Docker и Docker Compose установлены
- [ ] SSH доступ настроен
- [ ] `.env` файл создан и настроен
- [ ] `credentials.json` загружен
- [ ] Домен настроен (опционально)

### В GitHub:
- [ ] Репозиторий создан
- [ ] GitHub Actions настроен (для CI/CD)
- [ ] Secrets добавлены
- [ ] Workflow файл создан

---

## 🔄 Как деплоить

### Способ 1: Ручной (на сервере)

```bash
# Подключение к серверу
ssh user@your-server-ip

# Переход в директорию
cd BookHunter

# Обновление
./deploy.sh update
```

### Способ 2: Через Git (на сервере)

```bash
ssh user@your-server-ip
cd BookHunter
git pull origin main
docker compose down
docker compose up -d --build
```

### Способ 3: Автоматический (CI/CD)

```bash
# На вашем компьютере
git add .
git commit -m "feat: новая функция"
git push origin main

# ✅ Готово! GitHub Actions задеплоит автоматически
```

---

## 📊 Мониторинг

### Локально:
```bash
# Статус контейнеров
docker compose ps

# Логи приложения
docker compose logs -f app

# Все логи
docker compose logs -f
```

### На сервере:
```bash
# Подключение
ssh user@your-server-ip

# Статус
cd BookHunter
./deploy.sh status

# Логи
./deploy.sh logs app
```

### Через GitHub Actions:
1. Перейдите в `Actions` в репозитории
2. Посмотрите на последний запуск
3. 🟢 Зеленый — успешно
4. 🔴 Красный — ошибка

---

## 🐛 Решение проблем

### Контейнер не запускается:

```bash
# Проверка логов
docker compose logs app

# Перезапуск
docker compose restart app

# Пересборка
docker compose up -d --build
```

### Ошибка подключения к базе данных:

```bash
# Проверка PostgreSQL
docker compose logs postgres

# Перезапуск
docker compose restart postgres

# Проверка подключения
docker compose exec postgres psql -U bookuser -d book_discounts
```

### Деплой через CI/CD не работает:

1. Проверьте Secrets в GitHub
2. Проверьте SSH подключение
3. Посмотрите логи в GitHub Actions
4. Попробуйте ручной деплой на сервере

---

## 💾 Бэкап и восстановление

### Создание бэкапа:

```bash
# На сервере
cd BookHunter
./deploy.sh backup
```

### Восстановление из бэкапа:

```bash
# База данных
docker compose exec -T postgres psql -U bookuser book_discounts < backups/db_20260210_120000.sql

# Файлы
tar -xzf backups/files_20260210_120000.tar.gz
```

---

## 🔒 Безопасность

### Проверьте перед деплоем:

- [ ] Пароли в `.env` изменены на сложные
- [ ] `DEBUG=False`
- [ ] `credentials.json` в `.gitignore`
- [ ] `.env` в `.gitignore`
- [ ] SSL/HTTPS настроен
- [ ] Firewall настроен

---

## 📈 Масштабирование

### Увеличение ресурсов:

Отредактируйте `docker-compose.yml`:

```yaml
services:
  app:
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
```

### Несколько инстансов:

```yaml
services:
  app:
    deploy:
      replicas: 3
```

---

## ✅ Проверка после деплоя

1. **Главная страница:** https://yourdomain.com/web
2. **API Health:** https://yourdomain.com/api/health
3. **Админ-панель:** https://yourdomain.com/admin
4. **Mini App:** https://yourdomain.com/telegram
5. **Telegram Bot:** Отправьте /start

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker compose logs`
2. Проверьте статус: `docker compose ps`
3. Проверьте .env файл
4. Свяжитесь с поддержкой

---

## 🎯 Дополнительные ресурсы

- 📖 [Полная документация CI/CD](CI_CD_GUIDE.md)
- 📖 [Инструкция по деплою](DEPLOY_TO_SERVER.md)
- 📖 [Быстрый старт CI/CD](QUICK_DEPLOY.md)
- 📖 [README проекта](README.md)

---

**Удачи с деплоем!** 🚀
