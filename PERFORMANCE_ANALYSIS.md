# 📊 Оценка производительности проекта BookHunter

**Дата оценки:** 2026-02-18
**Версия проекта:** v1.0

---

## 1. Архитектура проекта

### Компоненты:
| Сервис | Версия | Роль | Ресурсы |
|--------|--------|------|---------|
| **FastAPI App** | 0.104.1 | API сервер | 4 workers |
| **Celery Worker** | 5.3.4 | Обработка задач | 2 workers |
| **Celery Beat** | 5.3.4 | Планировщик задач | 1 процесс |
| **PostgreSQL** | 15 | База данных | 1 контейнер |
| **Redis** | 7-alpine | Кэш + очередь | 1 контейнер |
| **FlareSolverr** | latest | Обход CloudFlare | 1 контейнер |
| **Telegram Bot** | 20.7 | Бот для уведомлений | 1 процесс |

---

## 2. Метрики производительности (из логов)

### Парсинг:
```
📊 Парсинг "python":
- Время: ~33 секунды
- API запросов: 1
- Товаров получено: 60
- Книг со скидкой: 10
- Записей в Google Sheets: 10
```

### Обновление токена:
```
🔑 Обновление токена:
- Время: ~17 секунд
- FlareSolverr запрос: 1
- Cookies получено: 42
- Токен проверен: ✅
```

### API запросы:
```
⚡ API запросы:
- Время ответа: ~0.5-2 секунды
- Успешность: ~100%
- Retry: до 3 попыток
```

---

## 3. Анализ производительности по компонентам

### ✅ Сильные стороны:

#### 1. **Асинхронная архитектура**
- FastAPI с `async/await` для всех HTTP запросов
- `aiohttp` для асинхронных запросов к API
- `asyncpg` для асинхронной работы с PostgreSQL
- **Итог**: высокая пропускная способность API

#### 2. **Кэширование**
- Redis для токенов и cookies
- TTL 24 часа для токена
- **Итог**: минимальное количество запросов к API

#### 3. **Параллельная обработка**
- 4 workers для FastAPI
- 2 workers для Celery
- **Итог**: одновременная обработка нескольких запросов

#### 4. **Queue-based обработка**
- Celery для фоновых задач
- Redis как брокер сообщений
- **Итог**: не блокирует основной API

---

### ⚠️ Узкие места:

#### 1. **Google Sheets API**
```
📉 Проблема:
- 10 книг = 10 отдельных запросов к Google Sheets
- Каждый запрос занимает ~1-2 секунды
- Итого: ~10-20 секунд только на запись

💡 Решение:
- Пакетная запись (batch update)
- Кэширование в PostgreSQL
- Асинхронная запись через очередь
```

#### 2. **FlareSolverr**
```
📉 Проблема:
- Время запроса: ~14 секунд
- Используется только для токена
- Тяжёлый контейнер (Chrome headless)

💡 Решение:
- Кэширование токена (уже есть)
- Альтернативные методы получения токена
- Несколько FlareSolverr инстансов
```

#### 3. **Rate limiting API Читай-города**
```
📉 Проблема:
- 1 запрос = 60 товаров
- Задержка между запросами: 2-5 секунд
- Максимум: ~720 товаров в минуту

💡 Решение:
- Пагинация для большего количества
- Параллельные запросы с разных IP
- Прокси для обхода ограничений
```

#### 4. **Отсутствие мониторинга**
```
📉 Проблема:
- Нет метрик производительности
- Нет алертов на падения
- Нет трассировки запросов

💡 Решение:
- Prometheus + Grafana
- Sentry для ошибок
- OpenTelemetry для трассировки
```

---

## 4. Расчёт пропускной способности

### Текущая пропускная способность:

```
📊 Парсинг:
- 10 книг за 33 секунды
- = ~18 книг/минута
- = ~1080 книг/час
- = ~25920 книг/день

📊 Обновление токена:
- 1 раз за 17 секунд
- = 3.5 раза/минута
- = 200 раз/час

📊 API запросы:
- 1 запрос за 0.5-2 секунды
- = 60-120 запросов/минута на воркер
- = 120-240 запросов/минута (2 воркера)
```

### Потенциальная пропускная способность (с оптимизацией):

```
🚀 После оптимизации:
- ~50 книг/минута (в 2.8x быстрее)
- = ~3000 книг/час
- = ~72000 книг/день
```

---

## 5. Ресурсы сервера

### Текущее использование (оценка):

| Ресурс | Использование | Лимит | Нагрузка |
|--------|--------------|-------|----------|
| **CPU** | 10-20% | 100% | 🟢 Низкая |
| **RAM** | 1-2 GB | 4-8 GB | 🟡 Средняя |
| **Disk** | 5-10 GB | 50+ GB | 🟢 Низкая |
| **Network** | 1-5 MB/s | 1 Gbps | 🟢 Низкая |

### Прогноз нагрузки (при увеличении):

| Сценарий | CPU | RAM | Network |
|----------|-----|-----|---------|
| 10 запросов/мин | 20% | 2 GB | 5 MB/s |
| 100 запросов/мин | 50% | 3 GB | 20 MB/s |
| 1000 запросов/мин | 100%+ | 5+ GB | 100+ MB/s |

---

## 6. Рекомендации по оптимизации

### 🚀 Высокий приоритет:

#### 1. **Оптимизация Google Sheets API**
```python
# Было: 10 отдельных запросов
for book in books:
    add_book_to_sheet(book)  # ~1-2 сек

# Стало: 1 пакетный запрос
batch_update_books(books)  # ~2-3 сек

# Эффект: 5-10x ускорение записи
```

#### 2. **Добавить мониторинг**
```yaml
# docker-compose.prod.yml
monitoring:
  image: prometheus
  ports:
    - "9090:9090"

grafana:
  image: grafana
  ports:
    - "3000:3000"
```

#### 3. **Кэширование результатов парсинга**
```python
# Кэшировать результаты в Redis на 1 час
cache_key = f"parse:{source}:{query}"
if cached := redis.get(cache_key):
    return cached
```

### 🎯 Средний приоритет:

#### 4. **Увеличить concurrency**
```yaml
# docker-compose.prod.yml
celery_worker:
  command: celery -A services.celery_app worker --concurrency=4  # было 2
```

#### 5. **Connection pooling**
```python
# aiohttp connection pool
connector = aiohttp.TCPConnector(
    limit=100,  # макс. соединений
    limit_per_host=20  # на хост
)
```

#### 6. **Оптимизация базы данных**
```sql
-- Добавить индексы
CREATE INDEX idx_books_source ON books(source);
CREATE INDEX idx_books_parsed_at ON books(parsed_at);
```

### 🔵 Низкий приоритет:

#### 7. **CDN для статических файлов**
#### 8. **Сжатие ответов (gzip)**
#### 9. **Лимиты запросов (rate limiting)**

---

## 7. План масштабирования

### Масштабирование по горизонтали:

```
📈 Уровни масштабирования:

1. 🟢 Текущий (1 сервер)
   - 10 запросов/мин
   - 1000 книг/день

2. 🟡 Средний (2 сервера + Load Balancer)
   - 50 запросов/мин
   - 5000 книг/день

3. 🔴 Высокий (кластер Kubernetes)
   - 500+ запросов/мин
   - 50000+ книг/день
```

---

## 8. Итоговая оценка

### Общая оценка производительности: **7/10**

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| **Архитектура** | ⭐⭐⭐⭐⭐ | Отличная асинхронная архитектура |
| **Масштабируемость** | ⭐⭐⭐⭐ | Хорошая, но можно улучшить |
| **Надёжность** | ⭐⭐⭐⭐ | Есть retry mechanism |
| **Мониторинг** | ⭐⭐ | Нет метрик и алертов |
| **Оптимизация** | ⭐⭐⭐ | Есть базовые оптимизации |
| **Кэширование** | ⭐⭐⭐⭐ | Redis для токенов |
| **База данных** | ⭐⭐⭐⭐ | PostgreSQL + индексы |
| **API производительность** | ⭐⭐⭐⭐ | Быстро, но можно быстрее |

---

## 9. Топ-3 быстрых победы:

### 1. ⚡ Пакетная запись в Google Sheets
- **Эффект**: 5-10x ускорение записи
- **Сложность**: 🟢 Низкая
- **Время**: 2-4 часа

### 2. 📊 Добавить Prometheus + Grafana
- **Эффект**: Видимость всех метрик
- **Сложность**: 🟡 Средняя
- **Время**: 4-6 часов

### 3. 🔄 Увеличить concurrency Celery
- **Эффект**: 2x быстрее обработка задач
- **Сложность**: 🟢 Низкая
- **Время**: 30 минут

---

## 10. Чек-лист для оптимизации

### Высокий приоритет (сделать в первую очередь):
- [ ] Пакетная запись в Google Sheets
- [ ] Добавить Prometheus + Grafana
- [ ] Кэширование результатов парсинга

### Средний приоритет (сделать после):
- [ ] Увеличить concurrency Celery
- [ ] Connection pooling для aiohttp
- [ ] Индексы в PostgreSQL

### Низкий приоритет (опционально):
- [ ] CDN для статических файлов
- [ ] Сжатие ответов (gzip)
- [ ] Rate limiting

---

## 11. История изменений

| Дата | Изменение | Автор |
|------|-----------|-------|
| 2026-02-18 | Первичная оценка производительности | Koda |

---

**Примечание:** Этот документ будет обновляться по мере развития проекта.
