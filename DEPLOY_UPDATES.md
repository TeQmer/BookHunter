# Инструкция по обновлению проекта на сервере

## Обзор процесса

Обновление проекта на сервере состоит из трёх основных этапов:
1. **Локально:** внести изменения, сохранить в Git, отправить в репозиторий
2. **На сервере:** получить изменения из Git
3. **На сервере:** пересобрать и перезапустить Docker контейнеры

---

## Этап 1: Работа на локальной машине

### Шаг 1.1: Внесите изменения в код

Внесите необходимые изменения в файлы проекта на вашем компьютере.

### Шаг 1.2: Проверьте статус изменений

```bash
git status
```

Вы увидите список изменённых файлов.

### Шаг 1.3: Добавьте изменения в индекс

```bash
# Добавить все изменённые файлы
git add .

# Или добавить конкретные файлы
git add файл1.py файл2.py
```

### Шаг 1.4: Создайте коммит с описанием

```bash
git commit -m "Краткое описание изменений"
```

**Примеры хороших сообщений коммита:**
- `git commit -m "Добавлена страница настроек профиля"`
- `git commit -m "Исправлена ошибка с поиском книг"`
- `git commit -m "Обновлён Dockerfile для продакшена"`

### Шаг 1.5: Отправьте изменения в Git репозиторий

```bash
git push
```

Если команда не работает, попробуйте:
```bash
git push origin main
```

---

## Этап 2: Подключение к серверу

### Шаг 2.1: Подключитесь по SSH

```bash
ssh пользователь@ip-адрес-сервера
```
# 1. Проверить структуру папки
ls -la /var/www/bookhunter/telegram/

# 2. Скопировать файлы из telegram/app/ в /var/www/bookhunter/telegram/
cp -r telegram/app/* /var/www/bookhunter/telegram/

# 3. Проверить, что файлы скопировались
ls -la /var/www/bookhunter/telegram/

# 4. Перезагрузить nginx
sudo nginx -s reload

# 5. Проверить статус nginx
sudo systemctl status nginx

**Пример:**
```bash
ssh root@185.100.200.300
```

### Шаг 2.2: Перейдите в директорию проекта

```bash
cd /path/to/BookHunter
```

**Если вы не помните точный путь:**
```bash
ls ~
cd BookHunter
# или
cd /var/www/BookHunter
```

---

## Этап 3: Получение изменений на сервере

### Шаг 3.1: Проверьте текущую ветку

```bash
git branch
```

Убедитесь, что вы на ветке `main` (или `master`).

### Шаг 3.2: Получите изменения из репозитория

```bash
git pull
```

Вы увидите сообщение о том, какие файлы были обновлены.

**Если возникают конфликты:**
```bash
git stash
git pull
git stash pop
```

---

## Этап 4: Обновление Docker контейнеров

### Вариант А: Обычное обновление (сохраняет данные)

**Остановите контейнеры:**
```bash
docker compose down
```

**Пересоберите и запустите контейнеры:**
```bash
docker compose up -d --build
```

**Проверьте статус контейнеров:**
```bash
docker compose ps
```

Все контейнеры должны быть в статусе `Up`.

### Вариант Б: Полное пересоздание (удаляет данные!)

⚠️ **Внимание!** Эта команда удалит все данные из базы данных!

```bash
docker compose down -v
docker compose up -d --build
```

**Используйте этот вариант только если:**
- Вы изменили структуру базы данных и хотите начать с нуля
- Вы понимаете, что все данные будут потеряны

---

## Этап 5: Проверка работы

### Шаг 5.1: Проверьте логи

```bash
docker compose logs -f
```

Нажмите `Ctrl + C`, чтобы выйти из просмотра логов.

**Проверить логи конкретного сервиса:**
```bash
docker compose logs -f web
docker compose logs -f db
```

### Шаг 5.2: Проверьте сайт

Откройте в браузере:
- `https://mybook-hunter.ru`

Убедитесь, что:
- Сайт загружается
- Новые функции работают
- Нет ошибок в консоли браузера (F12 → Console)

---

## Дополнительные сценарии

### Сценарий 1: Изменились переменные окружения (.env)

Если вы изменили файл `.env` локально, его нужно обновить на сервере вручную (он не коммитится в Git).

**На сервере:**
```bash
nano .env
```

Внесите изменения, сохраните (`Ctrl + O`, `Enter`) и выйдите (`Ctrl + X`).

**Перезапустите контейнеры:**
```bash
docker compose down
docker compose up -d
```

### Сценарий 2: Изменились требования к зависимостям (requirements.txt)

```bash
docker compose down
docker compose up -d --build
```

Docker автоматически переустановит зависимости при пересборке.

### Сценарий 3: Изменилась структура базы данных

**Вариант 1: Создайте миграцию (рекомендуется)**
```bash
docker compose exec web python manage.py makemigrations
docker compose exec web python manage.py migrate
```

**Вариант 2: Полное пересоздание базы данных (потеря данных!)**
```bash
docker compose down -v
docker compose up -d --build
```

### Сценарий 4: Нужно откатить изменения

**На сервере:**
```bash
git log
# Найдите хеш предыдущего коммита
git reset --hard хеш_коммита
docker compose down
docker compose up -d --build
```

---

## Полезные команды

### Git команды

| Команда | Описание |
|---------|----------|
| `git status` | Показать изменённые файлы |
| `git diff` | Показать изменения в файлах |
| `git log` | История коммитов |
| `git pull` | Получить изменения из репозитория |
| `git reset --hard HEAD~1` | Откатить последний коммит |

### Docker команды

| Команда | Описание |
|---------|----------|
| `docker compose ps` | Список запущенных контейнеров |
| `docker compose logs` | Логи всех контейнеров |
| `docker compose logs -f web` | Логи сервиса web (в реальном времени) |
| `docker compose down` | Остановить контейнеры |
| `docker compose up -d` | Запустить контейнеры в фоне |
| `docker compose restart` | Перезапустить контейнеры |
| `docker compose exec web bash` | Зайти в контейнер web |

### Команды для отладки

**Проверить свободное место на сервере:**
```bash
df -h
```

**Проверить использование ресурсов:**
```bash
htop
# или
top
```

**Проверить порты:**
```bash
netstat -tlnp
```

---

## Частые проблемы и решения

### Проблема: "Permission denied" при git pull

**Решение:**
```bash
sudo chown -R $USER:$USER .
git pull
```

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
docker compose logs
# Посмотрите логи и найдите ошибку
```

### Проблема: Сайт недоступен после обновления

**Решение:**
```bash
# Проверьте статус контейнеров
docker compose ps

# Проверьте логи
docker compose logs -f

# Перезапустите Nginx (если нужно)
sudo systemctl restart nginx
```

### Проблема: Ошибка с базой данных

**Решение:**
```bash
# Проверьте статус контейнера базы данных
docker compose ps db

# Посмотрите логи базы данных
docker compose logs db

# Если контейнер не запускается, попробуйте пересоздать без удаления данных
docker compose down
docker compose up -d
```

---

## Автоматизация (опционально)

Если вы часто обновляете проект, можно создать скрипт автоматического деплоя.

Создайте файл `deploy.sh` на сервере:

```bash
#!/bin/bash

echo "🔄 Получение изменений из Git..."
git pull

echo "🛑 Остановка контейнеров..."
docker compose down

echo "🔨 Пересборка и запуск контейнеров..."
docker compose up -d --build

echo "✅ Готово! Проверка статуса..."
docker compose ps

echo "📋 Логи:"
docker compose logs --tail=20
```

Сделайте скрипт исполняемым:
```bash
chmod +x deploy.sh
```

Теперь для деплоя достаточно одной команды:
```bash
./deploy.sh
```

---

## Чек-лист перед деплоем

- [ ] Все изменения закоммичены и отправлены в Git
- [ ] На локальной машине всё работает корректно
- [ ] Проверены изменения в `.env` (если были)
- [ ] Понятно, какие изменения вносятся в базу данных
- [ ] Есть план отката, если что-то пойдёт не так

---

## Контакты для помощи

Если возникли проблемы:
1. Проверьте логи: `docker compose logs -f`
2. Проверьте статус контейнеров: `docker compose ps`
3. Попробуйте перезапустить: `docker compose restart`
