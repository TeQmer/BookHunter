# 🔄 Руководство по CI/CD для BookHunter

Полное руководство по автоматическому деплою BookHunter через GitHub Actions.

---

## 📋 Что такое CI/CD?

**CI (Continuous Integration)** — непрерывная интеграция. Код автоматически тестируется и собирается при каждом пуше.

**CD (Continuous Deployment)** — непрерывное развертывание. Приложение автоматически деплоится на сервер после успешных тестов.

### Преимущества:

- ✅ **Автоматизация:** Никаких ручных действий
- ✅ **Скорость:** Обновления за несколько минут
- ✅ **Надежность:** Автоматические проверки перед деплоем
- ✅ **Откат:** Быстрый возврат к предыдущей версии
- ✅ **Безопасность:** Секреты хранятся в GitHub Secrets

---

## 🚀 Настройка GitHub Actions

### 1. Настройка SSH доступа к серверу

#### Генерация SSH ключей (на вашем компьютере):

```bash
# Генерация ключа
ssh-keygen -t rsa -b 4096 -C "github-actions" -f ~/.ssh/github_actions_key

# Копирование публичного ключа на сервер
ssh-copy-id -i ~/.ssh/github_actions_key.pub user@your-server-ip

# Тест подключения
ssh -i ~/.ssh/github_actions_key user@your-server-ip
```

#### Или ручная настройка:

```bash
# Копирование публичного ключа
cat ~/.ssh/github_actions_key.pub | ssh user@your-server-ip "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

### 2. Добавление Secrets в GitHub

Перейдите: `Repository → Settings → Secrets and variables → Actions`

Добавьте следующие секреты:

| Secret Name | Описание | Пример |
|-------------|----------|---------|
| `SSH_PRIVATE_KEY` | Приватный SSH ключ | Содержимое `github_actions_key` |
| `SERVER_HOST` | IP или домен сервера | `192.168.1.100` или `bookhunter.ru` |
| `SERVER_USER` | Пользователь на сервере | `root` или `ubuntu` |
| `DOMAIN` | Домен вашего сайта | `https://bookhunter.ru` |
| `TELEGRAM_BOT_TOKEN` | Токен бота (для уведомлений) | `123456:ABC-DEF...` |
| `TELEGRAM_CHAT_ID` | ID чата для уведомлений | `123456789` |

#### Как добавить секрет:

1. Перейдите в `Settings → Secrets and variables → Actions`
2. Нажмите `New repository secret`
3. Введите имя и значение
4. Нажмите `Add secret`

---

## 📝 Настройка файла workflows

Файл уже создан: `.github/workflows/deploy.yml`

### Что делает этот workflow:

1. **Checkout code** — Получает код из репозитория
2. **Setup SSH** — Настраивает SSH подключение к серверу
3. **Deploy to server** — Выполняет деплой на сервер
4. **Health check** — Проверяет работоспособность
5. **Notify** — Отправляет уведомление о результате

---

## 🚀 Как работает деплой

### Сценарий 1: Автоматический деплой при пуше

```bash
# Вы на вашем компьютере
git add .
git commit -m "feat: добавлена новая функция"
git push origin main
```

**Что происходит автоматически:**

1. GitHub получает пуш в ветку `main`
2. Запускается workflow
3. Код деплоится на сервер
4. Приложение перезапускается
5. Вы получаете уведомление

⏱️ **Время:** 2-5 минут

---

### Сценарий 2: Ручной запуск

1. Перейдите в `Actions` в репозитории
2. Выберите workflow `Deploy BookHunter to Server`
3. Нажмите `Run workflow`
4. Выберите ветку
5. Нажмите `Run workflow`

---

### Сценарий 3: Деплой из другой ветки

Измените `on:` в `.github/workflows/deploy.yml`:

```yaml
on:
  push:
    branches:
      - main          # Продакшн
      - staging       # Тестовый сервер
      - develop       # Разработка
```

---

## 🔍 Мониторинг деплоя

### Просмотр логов деплоя

1. Перейдите в `Actions`
2. Выберите запуск workflow
3. Кликните на job `deploy`
4. Просматривайте логи каждого шага

### Статусы деплоя:

- ⚪ **Pending** — Ожидание
- 🔵 **In progress** — Выполняется
- 🟢 **Success** — Успешно
- 🔴 **Failure** — Ошибка
- 🟡 **Cancelled** — Отменен

---

## 🐛 Решение проблем

### Ошибка: SSH connection failed

**Причина:** Неверный SSH ключ или настройки

**Решение:**
```bash
# Проверьте подключение вручную
ssh -i ~/.ssh/github_actions_key user@your-server-ip

# Проверьте права доступа
chmod 600 ~/.ssh/github_actions_key

# Проверьте authorized_keys на сервере
cat ~/.ssh/authorized_keys
```

### Ошибка: Docker compose failed

**Причина:** Ошибка при сборке или запуске контейнеров

**Решение:**
```bash
# Подключитесь к серверу
ssh user@your-server-ip
cd BookHunter

# Проверьте логи
docker compose logs app

# Попробуйте запустить вручную
docker compose up -d --build
```

### Ошибка: Health check failed

**Причина:** Приложение не отвечает после деплоя

**Решение:**
```bash
# Проверьте статус контейнеров
docker compose ps

# Проверьте логи
docker compose logs app

# Перезапустите приложение
docker compose restart app
```

---

## 🔄 Откат деплоя (Rollback)

### Способ 1: Через Git

```bash
# На вашем компьютере
git revert HEAD
git push origin main
```

GitHub Actions автоматически задеплоит предыдущую версию.

### Способ 2: Через Git Reset

```bash
# Сброс на предыдущий коммит
git reset --hard HEAD~1
git push origin main --force
```

⚠️ **Внимание:** Force push может быть опасен для командной работы!

### Способ 3: Ручной откат на сервере

```bash
# Подключитесь к серверу
ssh user@your-server-ip
cd BookHunter

# Сброс на предыдущий коммит
git reset --hard HEAD~1

# Перезапуск
docker compose down
docker compose up -d --build
```

---

## 📊 Улучшенный CI/CD

### Добавление тестов

Создайте `.github/workflows/test.yml`:

```yaml
name: Test BookHunter

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run tests
        run: |
          pytest tests/ -v
```

### Добавление линтинга

```yaml
- name: Run linter
  run: |
    pip install flake8 black
    flake8 .
    black --check .
```

### Добавление Docker Hub

```yaml
- name: Login to Docker Hub
  uses: docker/login-action@v2
  with:
    username: ${{ secrets.DOCKER_USERNAME }}
    password: ${{ secrets.DOCKER_PASSWORD }}

- name: Push to Docker Hub
  run: |
    docker push ${{ secrets.DOCKER_USERNAME }}/bookhunter:latest
```

---

## 📈 Мониторинг и алерты

### Интеграция с Sentry

Добавьте в `.github/workflows/deploy.yml`:

```yaml
- name: Notify Sentry
  uses: getsentry/action-release@v1
  env:
    SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
    SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
    SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}
```

### Интеграция с Slack

```yaml
- name: Notify Slack
  uses: 8398a7/action-slack@v3
  with:
    status: ${{ job.status }}
    webhook_url: ${{ secrets.SLACK_WEBHOOK }}
  if: always()
```

---

## ✅ Чек-лист перед первым деплоем

- [ ] Настроен SSH доступ к серверу
- [ ] Добавлены все Secrets в GitHub
- [ ] Создан файл `.github/workflows/deploy.yml`
- [ ] Проверено подключение по SSH
- [ ] Установлен Docker на сервере
- [ ] Настроен `.env` файл на сервере
- [ ] Проверен ручной деплой
- [ ] Настроены уведомления (опционально)

---

## 🎯 Рекомендации

### 1. Используйте отдельные ветки

```
main       → Продакшн (автоматический деплой)
staging    → Тестовый сервер (автоматический деплой)
develop    → Разработка (без деплоя)
```

### 2. Тестируйте перед деплоем

```bash
# Запуск тестов локально
pytest tests/

# Проверка линтинга
flake8 .
black --check .
```

### 3. Используйте Semantic Versioning

```
v1.0.0  → Major (изменения, несовместимые назад)
v1.1.0  → Minor (новые функции, обратная совместимость)
v1.1.1  → Patch (исправления ошибок)
```

### 4. Храните важные данные

Не удаляйте `.env` файл при деплое! Используйте:
```bash
# На сервере
git stash
git pull
git stash pop
```

---

## 📚 Полезные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [SSH Key Authentication](https://www.ssh.com/academy/ssh/key)

---

**Удачи с автоматизацией!** 🚀
